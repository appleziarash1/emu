# Production Asset Plan
This runnable package uses procedural/vector rendering so it is self-contained.
For a production art pass replace the vector layers with original sprite sheets,
realm backgrounds, enemy/boss animations, VFX, UI art, music and SFX.
Do not copy proprietary game assets.
